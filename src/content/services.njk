---
title: Practice Areas
layout: simple-page.njk
---
<h1>{{ title }}</h1>

{% for key, area in practiceAreas %}
  <h2>{{ area.name }}</h2>
  <p>{{ area.description | renderContent("md") | safe }}</p>

  <ul>
    {% for service in area.services %}
      <li>{{ service }}</li>
    {% endfor %}
  </ul>
{% endfor %}
